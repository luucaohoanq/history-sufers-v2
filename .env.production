# Environment variables for production deployment

# Domain (change this to your actual domain)
# Use localhost for testing, or your domain for production
DOMAIN=localhost

# Node environment
NODE_ENV=production

# Server port (internal - Caddy proxies to this)
PORT=3000

# Max players per room
MAX_PLAYERS=50

# Colyseus monitoring (optional)
# MONITOR_PASSWORD=your-secure-password

# Database (if you add one later)
# DATABASE_URL=postgresql://user:pass@host:5432/dbname
